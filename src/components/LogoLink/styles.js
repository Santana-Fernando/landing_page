import styled, {css} from 'styled-components';

export const Container = styled.a`
    text-decoration: none;
    color: inherit;
    align-items: center;
    display: flex;

    > img {
        max-height: 3rem;
    }
`;
